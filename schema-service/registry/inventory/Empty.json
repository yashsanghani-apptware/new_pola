{
  "$id": "http://registry.agsiri.com/v1/schemas/empty/2.20",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$schemaName": "empty",
  "$schemaVersion": "2.20",
  "$author": "Surendra Reddy",
  "$description": "Represents an empty activity in a Satori process, used as a placeholder or no-op action.",
  "$license": "Confidential Property of 451 Labs",
  "type": "object",
  "properties": {
    "type": { "enum": ["empty"] },
    "id": { "type": "string" },
    "name": { "type": "string" }
  },
  "required": ["type", "id"]
}

